version: '3.8'

services:
  receipt-bot:
    build: .
    container_name: receipt_ocr_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    deploy:
      resources:
        limits:
          memory: 1G